HUGGINGFACE_API_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
HUGGINGFACE_MODEL_DEFAULT=meta-llama/Llama-3.1-8B-Instruct
# Optional. Per-instance, per-IP grading request limit per minute. Default 60.
# Set to 0 only for offline benchmark runs (eval/), which disables the limiter.
GRADING_RATE_LIMIT_PER_MINUTE=60
